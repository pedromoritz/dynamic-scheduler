apiVersion: v1
kind: Service
metadata:
  name: pod-dynamic-scheduler
  namespace: lab1
spec:
  type: NodePort
  selector:
    app: pod-dynamic-scheduler
  ports:
    - protocol: TCP
      nodePort: 31001
      port: 8080
      targetPort: http 
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pod-dynamic-scheduler
  namespace: lab1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pod-dynamic-scheduler
  template:
    metadata:
      labels:
        app: pod-dynamic-scheduler
    spec:
      schedulerName: dynamic-scheduler
      containers:
      - name: customapp
        image: pedromoritz/customapp:latest
        ports:
          - name: http
            containerPort: 8080
