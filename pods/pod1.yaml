apiVersion: v1
kind: Service
metadata:
  name: pod1
  namespace: lab1
spec:
  type: NodePort
  selector:
    app: pod1
  ports:
    - protocol: TCP
      nodePort: 31001
      port: 8080
      targetPort: http 
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pod1
  namespace: lab1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pod1
  template:
    metadata:
      labels:
        app: pod1
    spec:
      schedulerName: ppgcc_scheduler
      containers:
      - name: customapp
        image: pedromoritz/customapp:latest
        ports:
          - name: http
            containerPort: 8080
        resources:
          limits:
            memory: "3Gi"
            cpu: "500m"